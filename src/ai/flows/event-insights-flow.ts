/**
 * @fileOverview Placeholder for event insights logic. 
 * Server-side flows are disabled for static export builds.
 */

export async function getEventInsights(input: any): Promise<any> {
  return { summary: "AI insights are currently disabled in the mobile build.", funFacts: [] };
}
